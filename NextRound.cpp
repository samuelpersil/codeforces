#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, m; cin >> n >> m;
    int numbers[n], passes =0;
    for(int i = 0; i < m; i++){
        cin >> numbers[i];
    }
    for(int i = 0; i < n; i++){
        if(numbers[i] <= ){
            passes++;
        }
    }
    cout << passes;
}